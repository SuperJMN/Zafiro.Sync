apiVersion: v1
kind: ConfigMap
metadata:
  name: appfilesync-api-config
  namespace: appfilesync
data:
  Authentication__RequireHttpsMetadata: "true"
  Authentication__AppIdentity__ChallengeLifetimeSeconds: "300"
  Authentication__AppIdentity__SessionLifetimeSeconds: "900"
  AppFileSync__MigrateOnStartup: "false"
  AppFileSync__Apps__0__AppId: "fifo-calculator"
  AppFileSync__Apps__0__DisplayName: "FIFO Calculator"
  AppFileSync__Apps__0__MaxPlaintextBytes: "5242880"
  AppFileSync__Apps__0__IsEnabled: "true"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: appfilesync-api
  namespace: appfilesync
spec:
  replicas: 1
  selector:
    matchLabels:
      app: appfilesync-api
  template:
    metadata:
      labels:
        app: appfilesync-api
    spec:
      containers:
        - name: api
          image: ghcr.io/your-org/appfilesync-api:latest
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8080
          envFrom:
            - configMapRef:
                name: appfilesync-api-config
            - secretRef:
                name: appfilesync-api-secrets
          readinessProbe:
            httpGet:
              path: /readyz
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 10
            periodSeconds: 30
---
apiVersion: v1
kind: Service
metadata:
  name: appfilesync-api
  namespace: appfilesync
spec:
  selector:
    app: appfilesync-api
  ports:
    - name: http
      port: 80
      targetPort: http
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: appfilesync-api
  namespace: appfilesync
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt
spec:
  tls:
    - hosts:
        - appfilesync.example.com
      secretName: appfilesync-api-tls
  rules:
    - host: appfilesync.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: appfilesync-api
                port:
                  name: http
